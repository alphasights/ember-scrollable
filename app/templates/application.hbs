{{as-sidebar
  currentUser=currentUser
  navigationItems=navigationItems
  isCollapsed=preferences.sidebarCollapsed
  toggleCollapse="onSidebarToggle"
  logout="logout"}}

<main>
  {{render "quick-jump" this tagName="section"}}

  {{#as-scrollable class="content"}}
    {{outlet}}
  {{/as-scrollable}}
</main>

{{outlet "side-panel"}}
