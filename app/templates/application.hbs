{{as-sidebar
  currentUser=currentUser
  navigationItems=navigationItems
  isCollapsed=currentUser.preferences.sidebarCollapsed
  toggle="onSidebarToggle"}}

<main>
  {{render "quick-jump" this tagName="section"}}

  {{#as-scrollable class="content"}}
    {{outlet}}
  {{/as-scrollable}}
</main>

{{outlet "side-panel"}}
