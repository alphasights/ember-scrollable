<ul>
  {{#each membership in memberships itemController="teams.team.angle-membership"}}
    <li>
      <article>
        {{as-avatar user=membership.user}}
        {{super-number value=membership.deliveryTarget class="delivery-target"}}
        <button class="remove" tabindex="-1" {{action "remove" membership bubbles=false}}></button>
      </article>
    </li>
  {{/each}}
</ul>

{{#rl-dropdown-container class="dropdown add"}}
  {{#rl-dropdown-toggle}}
      <i></i>
  {{/rl-dropdown-toggle}}

  {{#rl-dropdown tagName="ul" class="team-members" closeOnChildClick="article"}}
    <li class="search">
      {{#as-progress-indicator promise=requestPromise spinnerColor="#f88838"}}
        <i></i>
      {{/as-progress-indicator}}

      {{input type="text" value=query click=view.onSearchClick}}
    </li>

    <li>
      <ul>
        {{#each member in unusedTeamMembers}}
          <li {{action "add" member}}>
            <article>
              {{as-avatar user=member}}

              <div class="name">
                {{member.name}}
              </div>
            </article>
          </li>
        {{/each}}
      </ul>
    </li>
  {{/rl-dropdown}}
{{/rl-dropdown-container}}
