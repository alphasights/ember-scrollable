<header>
  <div class="whiteboard-select {{if showWhiteboardSelect 'open'}}">
    {{#if showWhiteboardSelect}}
      <div class="select">
        <select onchange={{action "changeSelectedWhiteboard" value="target.value"}}>
          {{#each groupedWhiteboards as |group|}}
            <optgroup label={{group.label}}>
              {{#each group.content as |whiteboard|}}
                <option value={{whiteboard.id}} selected={{eq whiteboard selectedWhiteboard}}>
                  {{whiteboard.name}}
                </option>
              {{/each}}
            </optgroup>
          {{/each}}
        </select>
      </div>
    {{else}}
      <h1>{{selectedWhiteboard.name}}</h1>
    {{/if}}

    {{#if multipleWhiteboardsAvailable}}
      <label><button class="toggle" {{action "toggleWhiteboardSelect"}}>Change Whiteboard</button></label>
    {{/if}}
  </div>
</header>

{{outlet}}
