<label {{bindAttr for=name}}>{{label}}</label>

<div>
  {{input type="text" name=name placeholder=placeholder value=value}}
</div>

<div class="error">
  {{#each error in errors}}
    {{error}}
  {{/each}}
</div>