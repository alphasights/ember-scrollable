{{#liquid-if promise.isLoading use="crossFade" class="liquid-spinner"}}
  {{as-spinner
    width=spinnerWidth
    length=spinnerLength
    radius=spinnerRadius
    color=spinnerColor}}
{{else}}
  {{yield}}
{{/liquid-if}}
