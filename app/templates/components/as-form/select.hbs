{{#as-form/field}}
  {{view "select"
    name=name
    content=content
    optionValuePath="content.id"
    optionLabelPath="content.name"
    prompt=prompt
    optionGroupPath=optionGroupPath
    value=value}}
{{/as-form/field}}
