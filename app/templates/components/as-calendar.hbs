<header>
  <div class="time-zone">
    <div>TZ</div>
  </div>

  <div class="days">
    <ul>
      {{#each day in days}}
        <li {{bind-attr style=dayStyle}}>
          {{moment day.date "ddd D/M"}}
        </li>
      {{/each}}
    </ul>
  </div>
</header>

<div>
  <ul class="time-slots">
    {{#each timeSlot in headerTimeSlots}}
      <li>{{moment timeSlot.time "h A"}}</li>
    {{/each}}
  </ul>

  <ul class="days">
    {{#each day in days}}
      <li {{bind-attr style=dayStyle}}>
        <ul class="times">
          {{#each time in day.times}}
            <li>
              {{as-calendar-time time=time calendar=this}}
            </li>
          {{/each}}
        </ul>
      </li>
    {{/each}}
  </ul>
</div>
