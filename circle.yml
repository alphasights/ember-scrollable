test:
  override:
    - ./bin/ci

dependencies:
  override:
    - npm install
    - bower install
    - bundle install

deployment:
  production:
    branch: master
    commands:
      - "git push --force git@heroku.com:as-phoenix-production.git HEAD:master"
