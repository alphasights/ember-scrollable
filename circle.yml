machine:
  node:
    version: 0.10.22

test:
  override:
    - ./bin/ci

dependencies:
  override:
    - npm install
    - bower install
    - bundle install
    # - node_modules/.bin/ember g ember-cli-paint

deployment:
  staging:
    branch: master
    commands:
      - "git push --force git@heroku.com:as-phoenix-staging.git HEAD:master"
