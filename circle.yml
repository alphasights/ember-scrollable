machine:
  node:
    version: 0.10.22

test:
  override:
    - ./bin/ci

dependencies:
  override:
    - npm install ember-cli
    - npm install
    - bower install
    - bundle install

deployment:
  staging:
    branch: master
    commands:
      - "ember divshot push staging token=$DIVSHOT_TOKEN"
