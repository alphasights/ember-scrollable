test:
  override:
    - ./bin/ci

dependencies:
  override:
    - npm install
    - bower install
    - bundle install

deployment:
  staging:
    branch: master
    commands:
      - "git push --force git@heroku.com:as-phoenix-staging.git HEAD:master"
